import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The hand-built static marketing site (ajr-homepage.html, about.html, …),
  // Assets/, and prototype/ still live at the repo root and are served by the
  // separate static preview for now. They'll be migrated into the Next app
  // (or public/) in a later step.
};

export default nextConfig;
